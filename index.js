const express=require('express');
const app=express();
const port=8000;
app.listen(port,function(err)
{ 
    gif(err)
    {
    console.log('Error');
    }
    else{
        console.log("Error is not present");
    }
})